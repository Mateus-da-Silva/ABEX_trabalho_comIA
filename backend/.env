NODE_ENV=development
PORT=3001

# Banco de dados MySQL (Docker local)
DB_HOST=localhost
DB_PORT=3307
DB_USER=clinica_user
DB_PASSWORD=clinica_pass_2024
DB_NAME=clinica_saas

# Redis (Docker local)
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=super_secret_jwt_key_change_in_production_2024
JWT_EXPIRES_IN=7d

# Frontend (CORS)
FRONTEND_URL=http://localhost:3000

# IA (opcional — necessário apenas para o módulo de IA)
ANTHROPIC_API_KEY=
